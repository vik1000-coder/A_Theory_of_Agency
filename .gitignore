.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
*.pyc
runs/*
!runs/.gitkeep
# downloaded third-party datasets (CC-BY etc.) — fetched via README, not redistributed here
data/validation/
# LaTeX build artifacts (keep .tex and .pdf)
report/*.aux
report/*.fls
report/*.fdb_latexmk
report/*.log
report/*.out
report/*.synctex.gz
report/build.log
